// Connected to the live Supabase project (ungdkbgweivotrezyase).
// The previous localStorage shim is kept in the repo but no longer used.
export { supabase, createSignupClient } from './supabaseClient';
